// Modules
// every file is module (by default)
// Modules - encapsulated code (only share minimum )

const gajendra = 'gajendra';
const durgesh = 'durgesh';

const sayhii = (name) => {
console.log(`Hello there ${name}`);
}

sayhii ('gaurav');
sayhii (gajendra);
sayhii (durgesh);